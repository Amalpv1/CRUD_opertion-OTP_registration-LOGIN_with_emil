import 'package:flutter/material.dart';

class sampleLog extends StatelessWidget {
  const sampleLog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('samplLog'),
      ),
    );
  }
}